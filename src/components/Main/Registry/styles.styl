:root:root .registry-wrapper
  .registry
    min-height 100px
    display flex
    align-items center
    justify-content center
    background #fff
    padding 20px
    box-shadow 0 1px 1px #b7b7b7
    i.icon:hover
      cursor pointer
    .statistics
      font-size 0.7em
      display flex
      align-items center