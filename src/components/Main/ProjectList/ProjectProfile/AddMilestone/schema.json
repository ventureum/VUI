{
  "definitions": {
    "obj": {
      "type": "object",
      "required": [
        "name",
        "reward"
      ],
      "properties": {
        "name": {
          "type": "string",
          "title": "Name",
          "default": "objective 1"
        },
        "type": {
          "type": "string",
          "title": "Type",
          "enum": ["type1", "type2", "type3"],
          "default": "type1"
        },
        "reward": {
          "type": "number",
          "title": "Regulation Reward",
          "default": 10
        }
      }
    }
  },
  "type": "object",
  "required": ["days", "objs"],
  "properties": {
    "days": {
      "type": "number",
      "title":"Length in days",
      "default": 60,
      "minimum": 60
    },
    "objs": {
      "type": "array",
      "title": "Objectives",
      "minItems": 1,
      "uniqueItems": true,
      "items": {
        "$ref": "#/definitions/obj"
      }
    }
  }
}
