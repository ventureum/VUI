{
  "schema":{
    "definitions": {
      "regulator": {
        "type": "object",
        "required": [
          "name",
          "votes"
        ],
        "properties": {
          "name": {
            "type": "string",
            "title": "Regulator Name"
          },
          "votes": {
            "type": "number",
            "title": "Delegated Votes",
            "default": 0
          }
        }
      }
    },
    "type": "object",
    "required": [],
    "properties": {
      "regulators": {
        "type": "array",
        "title": "Delegate Votes",
        "minItems": 0,
        "uniqueItems": true,
        "items": {
          "$ref": "#/definitions/regulator"
        }
      }
    }
  },
  "MilestoneSchema":{
    "definitions": {
      "obj": {
        "type": "object",
        "required": [
          "name",
          "reward"
        ],
        "properties": {
          "name": {
            "type": "string",
            "title": "Name",
            "default": "objective 1"
          },
          "type": {
            "type": "string",
            "title": "Type",
            "enum": ["type1", "type2", "type3"],
            "default": "type1"
          },
          "reward": {
            "type": "number",
            "title": "Regulation Reward",
            "default": 10
          }
        }
      }
    },
    "type": "object",
    "required": ["name", "days", "objs"],
    "properties": {
      "name": {
        "type": "string",
        "title": "Name",
        "default": "Milestone 1"
      },
      "days": {
        "type": "number",
        "title":"Length in days",
        "default": 60,
        "minimum": 60
      },
      "objs": {
        "type": "array",
        "title": "Objectives",
        "minItems": 1,
        "uniqueItems": true,
        "items": {
          "$ref": "#/definitions/obj"
        }
      }
    }
  }
}

